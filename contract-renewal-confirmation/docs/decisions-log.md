# 意思決定ログ

要件定義・設計の過程で決めたこと、なぜそう決めたかを記録する。実装中に「なぜこの仕様になっているのか」を追えるようにするための記録。新しい決定を行った場合は、末尾に追記する。

---

## 2026-07-17 スコープを「本人意思確認」ステップのみに限定

Excel台帳には見積書送付・契約書作成・郵送・明示書送付など、客先ごとに全く異なる事務手続きが含まれる。今回自動化するのは「本人へ更新の意向を確認する」ステップのみ。前後の手続きは対象外・従来通り手動。

理由：客先ごとの手続きの違いが大きすぎて、全体を自動化しようとすると際限なく複雑になる。まず最も定型的で自動化しやすい部分に絞った。

## 2026-07-17 「同意しない」ボタンを置かない

本人向け画面の選択肢は「本人承諾」「相談したい・確認したいことがある」の二択のみとし、「同意しない」ボタンは意図的に置いていない。

理由：契約継続可否という重い判断を、本人のワンクリックで確定させたくない。「相談したい」を選べば必ず人（アシスタント）との会話に渡る設計にすることで、明確な拒否も含めてすべて人間が扱う。

## 2026-07-17 本人向け画面に自由記入欄・タグを置かない

「相談したい」を選んだ際のコメント欄・タグ選択は、一度追加を検討したが最終的に見送った。

理由：入力欄を設けると、会社への要望・待遇不満等の受け皿として使われるリスクがある。全件に会社が対応できる保証がない状態でこれを設置すると、対応されなかった場合に従業員満足度の低下要因になりかねない。「相談したい」の詳細は個別確認（会話）で拾う。

## 2026-07-17 自動的な「みなし同意」を廃止

無回答を自動的に「同意」とみなす仕組みは作らない。

理由：初回送信は人が確認してから送るのに、一番重い判断（同意とみなす）だけが完全自動というのは設計として非対称。エスカレーション後は、契約開始日までにアシスタントが個別確認を試みる運用ルールで担保する。接触できなかった場合は実務上問題ないため、そのまま契約更新手続きを進める（ただしステータスとして記録は残す）。

## 2026-07-17 ステータスに「更新しない」を追加し、自動グレーアウトしない

個別確認の結果「更新したくない」という結論に至った場合を、独立したステータスとして記録する。一覧画面では「本人承諾」のように自動でグレーアウトせず、営業事務が「対応開始」操作をするまで目立つ表示のまま残す。

理由：後続の契約終了手続き（対象外の業務）に営業事務が着手し忘れないようにするため。

## 2026-07-17 「無反応のまま契約開始日到達」を内訳付きで記録し、四半期モニタリングのみ行う

個別確認できないまま契約開始日を迎えた場合、個別の通知はせず、四半期ごとに集計・モニタリングする。「一度も接触できなかった」場合と「個別確認はしたが未定のまま」の場合を区別して記録する。

理由：この設計の安全性は「1on1等で本人の意向が事前に拾われている」という外部の運用実態に依存している。この前提が将来弱まった場合に早期発見できるよう、発生率を追跡する。

## 2026-07-17 個別確認結果の記録と自動処理の停止を連動させる

個別確認結果として「本人承諾」または「更新しない」が記録された瞬間、以降のリマインド・エスカレーションは自動停止する。「保留」の場合は、送信前なら通常送信、送信後なら以降の通知のみ停止する。

理由：記録と自動停止が連動しないと、「もう対応済みなのにリマインドが届く」という事故が起きる。

## 2026-07-17 リンクを開く行為自体は意思表示にしない

メールセキュリティ製品によるリンクの自動先読みで、本人が見る前に意思表示が確定してしまう事故を防ぐため、GETリクエスト（リンクを開く）では状態を変更せず、遷移後の画面上での明示的なボタン操作（POST相当）でのみ意思表示を確定させる。

## 2026-07-17 送信ドメインは @e-gravity.co.jp

本人の多くがこのドメイン（さくらインターネット、既存の全社員メール）を使っており、見慣れないO365ドメイン（gravityoffice365.onmicrosoft.com）より信頼されやすいため。営業事務・アシスタント側はMSアカウントを保有しているため、営業事務向け画面はMSAL.js認証で問題ない。

## 2026-07-17 バックエンドはAWS、既存アカウント内に構築

GASは使わず、AWS上に新設する。新規AWSアカウントは作らず、既存の業務系・人材評価系と同じアカウント内に構築する（IAM権限は厳密に分離）。後日AWS Organizations等への移行を検討する余地は残す。

理由：このプロジェクトの規模に対して、新規マルチアカウント基盤をゼロから整備するのは過剰投資になり得る。

## 2026-07-21 バックエンドはサーバーレス構成、VPCは使わない

契約更新確認システムのバックエンドは、Lambda・DynamoDB・API Gateway・SES・SQS・EventBridgeによるサーバーレス構成とし、VPCは使わない。

理由：想定トラフィックが小規模(週次同期＋四半期ごとの数十〜百数十件)であり、VPC経由のRDS等を採用するとNAT Gateway費用や運用負荷が増える。Lambda/DynamoDB/SES/API Gatewayはいずれもリージョナルサービスで通信はTLSで完結するため、VPCなしでも要件書5章の「保存時・通信時の暗号化」は満たせると判断した。要件書にある「VPC内部通信の暗号化」は、VPC構成を採用する場合の網羅的なチェック項目として書かれたものと解釈し、VPCを使わない今回の構成では非該当と判断した。

## 2026-07-21 IaCはAWS CDK(TypeScript)を採用

理由：既存のフロントエンド資産(Vanilla JS)、および今回追加するLambdaの大半の実装言語(TypeScript、後述)と言語を揃えられる。Terraformのような外部state backendのブートストラップが不要で、CloudFormationの状態管理にそのまま乗れる点も運用負荷の低さにつながる。

## 2026-07-21 Lambda実装言語はTypeScriptを基本とし、SharePoint Excel取り込み(週次同期)のみPythonを採用する

CDK・大半のLambda(候補一覧・送信・本人回答受付・リマインド/エスカレーション・四半期集計等)はTypeScriptで実装する。SharePoint台帳(客先ごとに列構成が異なる自由帳票)を取り込む週次同期Lambdaのみ、Pythonで実装する。

理由：客先ごとに構造が異なる不定形の表データを扱う工程は、pandas/openpyxlなどPythonのエコシステムが持つ表データ処理の強みが直接活きる。他の工程は状態遷移や送信制御といった業務ロジック中心で、CDKと言語を揃えるメリット(型定義の使い回し)の方が大きいと判断した。Lambda単位で言語を分けることは一般的な構成であり、混在によるデプロイ上の追加コストは小さいと判断した。

## 2026-07-21 データストアはDynamoDB(現在状態テーブル＋監査ログテーブルの2本立て)

各対象者の「現在の状態」を持つテーブルと、状態変化のたびに追記する監査ログ(誰が・いつ・何をしたか)テーブルを、別々のDynamoDBテーブルとして持つ。

理由：想定されるアクセスパターン(ステータス別一覧表示、四半期集計)が事前に把握できており、GSIで対応可能。目的①「エビデンスを残すこと」を、監査ログテーブルによって直接担保する。SharePoint台帳が正データであるという既存方針(2026-07-17決定)とも整合し、このDBはあくまで進行中の意思確認ワークフローの状態を一時的に持つ位置づけとする。

## 2026-07-21 個別確認結果(4値セレクトボックス)の記録は上書き自由とし、変更履歴は監査ログで担保する

「本人承諾」「更新しない」を一度記録した後も、営業事務による訂正のための再編集を制限しない。ロックや承認フローは設けない。

理由：早期実装を優先する方針とした。変更のたびに監査ログテーブルへ「誰が・いつ・何から何に変更したか」を記録することで、エビデンスとしての要件は満たせる一方、追加の承認フロー等を作るコストは避けられる。ただし「本人承諾」「更新しない」から他の値へ変更すると自動リマインド・エスカレーションが再開されうるため、営業事務向け画面側には低コストな確認ダイアログ(ブラウザネイティブのconfirm等)を設け、誤操作を軽減する。

## 2026-07-21 Excel台帳の「本人確認」列取り込みは、値を丸めず出所を監査ログに残す

週次同期でExcel台帳の「本人確認」列を取り込む際、正規化後の値（confirmed等）だけを記録するのではなく、元の表記（例：「本人了承済み」「了承みなし」）と、どの客先ブロック由来かという出所情報を監査ログテーブルに残す。

理由：目的①「エビデンスを残すこと」に照らすと、正規化後のフラグだけでは、後から「実際にどのような経緯で確認が取れたか」を追跡できない。特に「了承みなし」（SSSブロック）は、営業事務からの個別連絡ではなく、社内の別ルート（リーダー経由の事前確認）に由来する確認結果であり、この由来を記録として残す必要があると判断した。

## 2026-07-21 週次同期で未知の「本人確認」値に遭遇した場合は「未解決」扱いとし、自動では確定させない

Excel台帳の「本人確認」列に、これまで確認されていない表記の値が出現した場合、自動的に「confirmed」とは判定せず、常に「未解決」として扱う。未解決の行は、候補生成の除外判定には使わない（＝通常通り意思確認フローに乗せる）。

理由：自動的な「みなし同意」を作らないという既存の設計原則（2026-07-17決定）を、Excel取り込みの入り口でも一貫させるため。将来、客先ごとに異なる表記が増えた場合でも、人が値の意味を確認するまでは安全側に倒す。

## 2026-07-21 同一人物の複数契約期間行は、期間が重複しないことが保証されているため追加の重複排除ロジックは不要

Excel台帳上、同一人物が複数の契約期間行を持つケース（例：いすゞ・金子氏の7-9月期と10-12月期）が存在するが、業務上の構造的制約により、同一人物が同時期に複数契約を持つ状態は発生しない（対象期間が必ず異なる）ことが確認された。

理由：要件書4.2の「1.5ヶ月前を基準日に候補抽出」という日付ベースの抽出ロジックであれば、この構造的保証により、追加の重複排除ロジックなしで意図通りに1人1候補が抽出される。

## 2026-07-21 「委託」種別は原則候補抽出の対象外とするが、客先ブロック単位の許可リストで例外を持たせる

契約種別が「委託」の行は、要件書1章の除外規定（個人事業主・一人社長法人等）に該当することが多いため、原則として候補抽出の対象外とする。ただし、客先によっては正社員が準委任契約で常駐しており、派遣と同様に意思確認の対象に含めるべきケースが稀に存在する（現時点ではイーソル株式会社のみ）。過去に富士ソフト株式会社で同様のケースが一度あったが、当時のみの終了済み契約であり、現行のExcel台帳にアクティブな行としては存在しないことを確認済み。

Excel台帳の「委託」という値だけからは、個人事業主なのか正社員の準委任なのかを判別できないため、`ledger_mapping.json` に「委託でも意思確認の対象に含める客先」の許可リストを持たせ、リストにない客先の「委託」行はデフォルトで除外する。現時点の許可リストはイーソル株式会社のみとする。まだ実在しない想定上の客先（検討段階のもの）は、実際にExcel台帳へ該当行が現れるまで設定に含めない。

除外漏れ・除外過多の両方向のリスクがあるため、四半期モニタリング（要件書4.8）の対象に「委託種別で除外された行数」を客先別に追加し、許可リストへの追加漏れを早期発見できるようにする。この項目は、Round 4（週次同期・候補抽出ロジック）着手時に `ledger_mapping.json` へ反映する。

## 2026-07-28 Excel台帳の「本人確認」列に既に解決済みの値がある行は、候補作成時点で「送信前の個別確認」記録済みとして扱う

Excel台帳の「本人確認」列に、週次同期の時点で認識済み（confirmedと判定できる）の値が入っている行は、候補（現在状態テーブルのレコード）が作成される際、営業事務が画面上で「送信前の個別確認」（要件書4.4）を記録したのと同じ状態として扱う。具体的には、個別確認結果フィールドに「本人承諾」相当の値をあらかじめセットした状態で候補レコードを作成し、4.7の自動停止条件がそのまま適用されて送信対象から自動的に除外される。記録の出所は「Excel台帳（週次同期）」であることを監査ログに残し、営業事務が画面上で記録したものと区別できるようにする。

未知の値（未解決）の場合はこの扱いを適用せず、通常通り候補として作成し、意思確認フローに乗せる（2026-07-21決定「週次同期で未知の本人確認値に遭遇した場合は未解決扱いとする」と整合）。

理由：要件書4.4の趣旨（1on1等により既に結果が分かっている場合は送信せずに個別確認結果を記録できる）と、SSSブロックの「了承みなし」ケースの扱い（個別確認の結果を営業事務以外の経路で得た場合も同様に扱う、2026-07-21決定）を踏まえ、Excel台帳に既に解決済みの値が入っている行についても、新システムの候補作成時点で同じ枠組みに載せることが整合的と判断した。

## 2026-07-28 テスト実行に伴う監査ログは削除せず保持する

Round 4後処理でDynamoDBに投入したテストデータ（synthetic・sanitized由来、計47件）を削除する際、現在状態テーブルのレコードは削除するが、対応する監査ログテーブルのイベント（EXCEL_SYNC等）は削除せず保持することとした。

理由：目的①「エビデンスを残すこと」に照らすと、テストとして行われた実行そのものも記録として残す方が整合的である。監査ログを削除しても運用上の支障はない（現在状態テーブルにレコードがなければ、次回同期時に重複判定の対象にもならない）一方、削除には追加のスキャン・削除操作が必要でコスト対効果が低い。今後のRoundで同様にテストデータを投入・削除する場面でも、同じ方針（現在状態テーブルのみ削除、監査ログは保持）を踏襲する。

## 2026-07-28 本番アラームが発報しうる開発中のテストは、実行前にclaude.aiスレッドへ申告する

Round 4後処理でのSharePoint接続テスト（実装方式の試行錯誤）により、本番のCloudWatchアラーム（contract-renewal-sync-errors）が実際に発報し、通知先メールアドレスに届く事象があった。原因は試行錯誤中の一時的なLambdaエラーであり、実運用障害ではないことを確認済み。

今後、Lambdaの手動実行を伴うテストで、エラー発生により本番アラームが発報する可能性がある場合は、実行前にclaude.aiスレッドへ一言申告してから実施する運用とする。理由：追加のインフラ変更（テスト用アラーム無効化フラグ等）を伴わず、工数ゼロで「アラート疲れ」による見逃しリスクを避けられるため。

## 2026-07-29 将来、契約更新確認システムはグラナビ本体への統合が構想されている（現時点では構想段階、設計変更は行わない）

グラナビ本番システムの開発者（田中宏和氏）より、契約更新確認システム（本プロジェクト）を将来的にグラナビ本体（田中氏の管理するAWS上のRDB・システム）へ統合する構想があることが判明した。現時点ではあくまで構想段階であり、時期・範囲とも未確定。

この情報を踏まえても、現時点の技術設計（独立したAWSアカウント内リソース、DynamoDBでのデータ保持、既存リソースからのIAM厳密分離等、2026-07-21決定）は変更しない。理由：構想が具体化・確定した時点で改めて移行計画を立てるべきであり、未確定の将来計画に基づいて現在の設計を先回りして変更することは、要件があいまいな段階での過剰な作り込みにつながるため。

ただし、この将来計画は「所属チーム情報の連携方式」の判断（2026-07-29決定、手動CSVエクスポートを選択）に影響している。当面軽量な連携（手動エクスポート）で済ませるという判断は、将来的な統合可能性を踏まえると合理的である。

## 2026-07-29 所属チーム情報は、グラナビ本番RDB（社員マスター）からの手動CSVエクスポートで連携する

営業事務向け一覧画面（要件書4.11・7.4）に表示する「氏名・チーム」のうち、チーム情報はExcel契約台帳に含まれていないため、グラナビ本番のRDB（PostgreSQL、社員マスター）から連携する。

連携方式は、RDBへの直接接続（read replica等）ではなく、田中宏和氏による**手動CSVエクスポート**とする。理由：
- グラナビ本体への将来的な統合構想（同日決定）を踏まえると、常時接続する密結合の連携基盤に投資する必要性は低い
- 本プロジェクトが一貫して採用してきたサーバーレス・VPCなし構成（2026-07-21決定）を維持でき、田中氏側のRDBへの常時アクセス経路を新たに開けずに済む

具体的な連携仕様：
- 田中氏が、社員番号・氏名・所属チーム名の3列のCSVを、契約更新確認システム専用のS3バケット（`contract-renewal-*`）内の固定パスに、不定期に配置する
- 突合キーは氏名とする。表記ゆれ等で一致しない場合は、エラーとせずチーム欄を空欄として処理を継続する
- 週次同期Lambdaの実行時にこのCSVを読み込むが、ファイルが存在しない場合・一部氏名が見つからない場合も処理全体を止めない（2026-07-21決定「週次同期で未知の本人確認値に遭遇した場合は未解決扱いとする」と同じ、安全側に倒す設計原則を踏襲）

## 2026-07-31 営業事務向け画面の認証方式は、IDトークンではなくアクセストークン＋カスタムスコープ（自己参照パターン）を採用

営業事務向け画面（ops-console.html）がバックエンドAPI（AWS API Gateway）を呼び出す際の認証方式として、SPAのIDトークンをそのまま使う方式ではなく、同一アプリ登録内で「APIの公開」によりカスタムスコープ（access_as_user）を定義し、アクセストークンを取得する方式を採用した。

IDトークンの本来の目的はクライアントアプリ自身の本人確認であり、リソースサーバーへの認可の証明としては設計意図から外れる（audienceがSPA自身のクライアントIDになるため）。Microsoft公式ガイダンスでも、API呼び出しにはIDトークンではなくアクセストークンの使用が推奨されている。

追加のスコープ定義・自分自身を「承認済みのクライアント アプリケーション」として事前登録する対応のみで実現でき、運用の手間はIDトークン方式とほぼ変わらないため、正式な方式（b案）を選んだ。この「クライアントとAPIが同一アプリ」という自己参照パターンは、Microsoft公式のSPA+自前APIサンプル実装でも採用されている定番パターンである。

## 2026-07-31 Bashコマンドの権限確認が頻発する問題に対応するため、.claude/settings.json に読み取り専用コマンドの許可リストを追加した

実装前提のファイル存在チェックやAWSリソース確認など、副作用のない読み取り系コマンドにも毎回権限確認ダイアログが出る状態を解消するため、リポジトリ直下の `.claude/settings.json`（Git管理・チーム共有）に `permissions.allow` ルールを追加した。

許可リストに含めた対象：
- ファイル・shell系の読み取り確認（`test`、`[` 等）
- AWSリソースの構成確認（`cloudformation describe/list`、`lambda get/list`、`logs describe/get/filter`、`events list/describe`、`scheduler get/list`、`sns list`、`sqs get/list`、`secretsmanager describe/list`、`iam simulate/get/list`）
- DynamoDB実データ閲覧（`scan`、`query`、`get-item`、`batch-get-item`）
- S3オブジェクト参照（`s3 ls`、`s3api list-objects-v2`、`head-object`、`get-object`）
- CDK読み取り系（`diff`、`synth`、`list`）

意図的に許可リストに含めなかった操作（引き続き都度確認）：
- 認証情報の取得（`secretsmanager get-secret-value`）— 決定ログ上「認証情報はCodeを介さずユーザーが直接投入する」方針のため
- 送信系（`ses`、`sns publish`、`sqs send-message`）— 本番のメール送信・通知を伴うため
- Lambdaの実行（`lambda invoke`）— 2026-07-28決定の「事前申告してから実施」という運用と整合させるため
- 書き込み・削除系（`dynamodb put/update/delete-item`、`cloudformation update/delete-stack`、`cdk deploy` 等）
- `git push`

上記の「都度確認のまま残す」対象については `deny`（完全ブロック）ではなく `allow` リスト外とした。理由：`deny` にすると必要な場面でユーザーが意図的に許可することもできなくなり、2026-07-28決定の「事前申告してから手動実行」という運用と矛盾するため。

## 2026-07-31 change-instructions.md への反映は、Codeによる直接編集を条件付きで許可する

これまで（2026-07-28決定）、change-instructions.md はclaude.aiスレッドのみが編集し、Claude Codeは編集・追記しない運用としてきた。理由は、claude.aiスレッドがこのファイルの現状を直接読む手段を持たないため、「claude.aiスレッドが完成形を作る→ユーザーが手動で貼り付ける→claude.aiスレッドが照合する」という三者間チェックで整合性を担保する必要があったため。

しかし実際の運用では、ユーザーによる手動貼り付け自体が誤りの原因になりうることが判明した（2026-07-31、Round外タスクの反映において、複数回の差分指示のうち一部が未反映のまま次の指示が積み重なる事象が発生）。一方で、Claude Codeは`.claude/settings.json`のような機械的に照合可能なファイルについては、指示通りの内容を正確に反映できることも確認済みである（同日、47件の許可リストを`cat`生出力で照合し一致を確認）。

これを踏まえ、change-instructions.md についても、claude.aiスレッドが提示した完成形をClaude Codeが直接書き込む方式を認めることとした。ただし、内容の確定権限はあくまでclaude.aiスレッドにあり、Codeの役割は指示された文字列を正確に反映する機械的な作業に限定する。Codeの自己申告（「反映できています」等の要約）は今回のRound外タスクの報告でも実際の件数と食い違ったことがあるため信頼せず、編集のたびに`cat docs/change-instructions.md`の生出力を必須で報告させ、claude.aiスレッド側が全文照合してから次の作業に進む運用とする。三者間チェックのうち「ユーザーの手作業」を「Codeの機械的反映＋生出力照合」に置き換える形であり、独立した照合という構造自体は維持する。

## 2026-07-31 .claude/settings.json の許可リストにPowerShell形式47件を追加（Bash形式のみでは主シェルに効いていなかった是正）

2026-07-31に追加した47件の `Bash(...)` 許可ルールは、この環境の主な実行経路（PowerShellツール）では AWS/CDK系コマンドにほぼ効いていなかった。`Bash(...)` と `PowerShell(...)` はツール別に独立した許可モデルであり、Bashツール経由の実行のみがカバーされていたため。なお、初回追加時点でCLAUDE.mdを確認したところ「PowerShellが主シェルである」という記載はなく、この情報はClaude Code起動時に自動注入される環境情報（システムプロンプト）に含まれていたものであった（リポジトリ内のファイルとしては確認できない）。決定ログに「CLAUDE.mdに明記されていた」と誤って記録していたため、2026-07-31付でこの一文を訂正する。初回追加時に、根拠となる情報の出所を確認せずに実装した点を実機検証不足として記録する。

是正として、同じ対象コマンド群（AWS describe/list/get系、DynamoDB scan/query/get-item、S3 ls/get-object、CDK diff/synth/list）をPowerShell形式（`PowerShell(...)`）でも追加し、Bash/PowerShell両方の実行経路をカバーした。加えて `PowerShell(Test-Path *)` と `PowerShell(Get-Content *)` を追加（今回の是正作業中に `.claude/settings*.json` の確認がPowerShell経由で行われることが判明したため）。結果として許可リストは合計94件（Bash形式47件＋PowerShell形式47件）となった。

読み取り専用に限定する方針・除外対象（書き込み・削除・送信系、`secretsmanager get-secret-value`、`lambda invoke`、`cdk deploy`、`git push`）は変更しない。

## 2026-07-31 ドキュメント運用ルールを3点確定した（コミットハッシュ非埋め込み・報告形式・照合粒度）

Round外タスク（Bashコマンドの権限確認頻発対応）を通じて、以下3点のドキュメント運用ルールが確定した。

**① ドキュメント本文へのコミットハッシュ埋め込みをやめる**：`change-instructions.md` 等の本文に「コミット `xxxxxxx`」のような表記を使っていたが、コミットハッシュはそのコミットの内容から計算されるため、コミット自身のハッシュをそのコミットの本文に書くことは原理的に不可能である（自己参照問題）。これまで `7f07a98` 等のハッシュ参照が成立して見えていたのは、別の後続コミットで追記されていたからに過ぎない。今後は、ドキュメント本文にコミットハッシュを埋め込まず、変更の追跡は `git log` の説明的なコミットメッセージに委ねる。

**② 完了報告は返信本文に直接貼り付ける**：これまで `cat` や `git diff` 等のツール実行結果を折りたたみパネルに任せていたが、展開しないと中身が見えない状態では照合作業でコピー漏れが生じる。今後は、確認のために提示する出力はすべてCodeの返信本文にコードブロックとして直接貼り付ける。

**③ 照合の粒度は変更規模で使い分ける**：新規セクション追加等の大きな変更は `cat` による全文出力、軽微な1行修正等は `git diff` による差分出力で足りる。どちらも②のルールに従い、返信本文に直接貼り付ける。

## 2026-07-31 Round 5は営業事務向け画面（ops-console.html）のUI・参照系APIまでとし、実際のメール送信処理（SES連携）はRound 6に持ち越す

Round 5では、候補一覧表示、個別確認結果の記録、緊急停止、担当者メモ、対応開始操作など画面操作に付随するAPI（GET/PATCH系）までを実装範囲とする。「選択した対象へ送信」ボタンとそれに伴う実際のSES送信処理・DynamoDBへの送信日時記録は、SES本番送信設定（SPF/DKIM/DMARC）とあわせてRound 6で実装する。Round5時点でのボタンの挙動は、チェックボックス選択・件数プレビュー・確認ダイアログまでをフロントエンドのみで実装し、確定操作後はバックエンドを呼び出さず「送信機能はRound 6で実装予定」の旨を表示するにとどめる。

理由：SES送信ドメイン設定（DNS）は社内の別担当者との調整が必要で、コーディング外の待ち時間が発生しうる。画面・参照系APIとメール送信という性質の異なる実装を1つのRoundに混在させず、緊急停止・個別確認記録といった運用に必要な機能を先に使える状態にすることを優先した。確認ダイアログのUX（要件書4.3のMVP必須項目）はRound5のうちにフロントエンドのみで検証し、Round6で置き換わる送信APIの使い捨て実装は避ける。

## 2026-07-31 チーム情報CSV連携（週次同期Lambda拡張）はRound5に含める

2026-07-29決定で連携方式（田中氏による手動CSVエクスポート、週次同期Lambda実行時の読み込み）を決めていたが、実装タイミングは未定だった。Round5（営業事務向け画面）で一覧のteam列を表示する必要があるため、Round5のスコープに含めることとした。

Python製の週次同期Lambdaへの変更であり、TypeScript製のops-console API群とはコードベースが異なる。着手前に、対象CSVファイルが実際にS3の想定パスへ配置済みかをCodeに確認させ、未配置の場合は「ファイルなし」を想定した動作（2026-07-29決定通り、処理全体を止めずteam欄を空欄のまま継続）で問題ないことを前提とする。

## 2026-07-31 緊急停止機能はトグル（再開可能）とし、個別確認結果と同じ「上書き自由＋監査ログ担保」方針に揃える

要件書4.12の緊急停止機能について、一方向（停止のみ、解除不可）ではなくトグル（ON/OFF双方向に操作可能）として実装する。ON/OFFいずれの操作も、実行者・日時を監査ログに記録する。

理由：緊急停止の典型的な用途（客先都合による契約消滅等）は、後から状況が覆る可能性がある。2026-07-21決定「個別確認結果の記録は上書き自由とし、変更履歴は監査ログで担保する」と同じ設計思想を緊急停止にも適用する方が一貫性がある。一方向にした場合、解除したいときにレコードを作り直す等の迂回策が必要になり、かえって運用が複雑になる。

## 2026-07-31 担当者メモは単一フィールド（上書き）とし、複数エントリのログ形式UIはMVP+1へ後回しにする

要件書4.9の担当者メモは、画面上は単一の自由記入欄（上書き型）として実装する。編集のたびに変更前後の内容を監査ログへ記録することで、目的①（エビデンスを残すこと）は監査ログ側で担保する。複数エントリを時系列で追記・一覧表示するログ形式のUIは、要件書の文言（単数の自由記入欄）に忠実ではなく実装コストも増えるため、MVP+1候補としてバックログに送る。

理由：早期実装を優先しつつ、監査ログによって変更履歴自体は失われない設計とすることで、目的①を損なわずに画面側のコストを抑えられると判断した。

## 2026-07-31 「対応開始」記録は、個別確認結果が編集されるたびに自動的にリセットする

営業事務向け一覧画面の「対応開始」操作（要件書4.11・7.4）について、一度記録した後に個別確認結果（4値セレクトボックス）の値が編集された場合、対応開始の記録（acknowledgedAt）は自動的にクリアし、再度の「対応開始」操作を必要とする仕様とする。終端ステータス再突入時のみリセットする案、一度記録したら固定する案も検討したが、判定ロジックの単純さと、このプロジェクトが一貫して採用してきた「安全側に倒す」設計原則（2026-07-17決定「みなし同意の廃止」等）を優先し、編集イベントごとに一律リセットする方式を採用した。

理由：2026-07-17決定「更新しない」ステータスを自動グレーアウトしない設計の趣旨は「営業事務が着手し忘れないようにするため」であり、記録が固定のままだと、値の訂正によって再び終端ステータスに戻った際に着手し忘れのリスクが再発しうる。個別確認結果の編集自体が営業事務による能動的な操作であり頻度も高くないため、単純なルールにしても運用負荷は小さいと判断した。

## 2026-07-31 週次同期実行時、チーム連携用CSVが存在しない場合はteamフィールドを上書きしない

teamフィールドの週次同期時の扱いについて、CSVが存在する場合は毎回全レコードへ上書きするが、CSVが存在しない場合はteamフィールドを更新せず、既存の値（初期状態ではnull）を維持する方式を採用する。

理由：組織変更（チーム異動）に追随するため、CSVが存在する限り毎回上書きする方式が望ましい一方、CSVが用意される前の週次同期実行によって、全レコードのteamが一律nullで上書きされてしまう事故を避ける必要がある。Codeからの実装提案（2026-07-31、Round5事前確認）を踏襲した。

## 2026-07-31 Round5のバックエンド完了時点で、第三者レビューを挟む

Round5はこのプロジェクトで初めて、実際の認証境界（JWT Authorizer）とDynamoDBスキーマ変更（新規4フィールド）を伴う実装である。バックエンド（推奨コミット単位1〜7）完了後、フロントエンド（8）着手前に、実装内容を別スレッドでの第三者レビューに回すこととした。

理由：影響範囲が広い変更の前は別スレッドでのレビューを検討するという、このプロジェクトの運用方針に基づく。Round5はこれまでのRoundにはなかった性質（認証・スキーマ変更）を含むため。

## 2026-07-31 GET /api/candidatesはデフォルトで全件返す（四半期スコープにしない）。`quarter`パラメータは任意の絞り込みとして残す

Round5実装中に判明した設計上の食い違いとして、`GET /api/candidates`の実装がGSI1（四半期をパーティションキーとする）を用いたクエリとなっており、`quarter`パラメータ省略時は「現在の四半期」のみを返す仕様になっていた。これは、このスレッドが想定していた「全件取得（フィルタ・ページングなし）」（Round5設計時点の想定）と異なる。

この差異を是正し、デフォルト（`quarter`パラメータ省略時）ではDynamoDBテーブルへの全件スキャン（Scan API）で全候補を返す方式に変更する。`quarter`パラメータを明示的に指定した場合のみ、GSI1を使ったクエリで絞り込む（将来の四半期別集計（Round9想定）等での再利用を見込み、GSI1・関連コードは削除しない）。

理由：候補作成時期（契約開始日の1.5ヶ月前）と契約開始日（quarterの起点）は四半期境界をまたぐため、デフォルトを四半期スコープにすると、契約開始日が近い将来の候補や、四半期をまたいで未解決のまま残っている候補（「無反応のまま契約開始日到達」等）が、一覧のデフォルト表示から漏れる。これは2026-07-17決定「『更新しない』ステータスを自動グレーアウトしない」設計の趣旨（営業事務が着手し忘れないよう、能動的に手放すまで一覧に残す）と衝突する。テーブル規模（現状44件、想定でも数十〜百数十件）を踏まえると、Scanによる性能・コストの懸念は小さいと判断した。

## 2026-07-31（付記）GSI3 nonKeyAttributesの`emergencyStop`表記は、Round3基盤構築時の仮置きだったことが判明

Round5のバックエンド実装中、DynamoDB GSI3のnonKeyAttributesに`emergencyStop`という名称が既に存在していたことが判明した。これはRound3（基盤構築、コミット`21ec06c`）の段階で、将来追加予定のフィールドを見越して仮置きされていたものであり、当時のdecisions-log.mdには記録されていなかった。Round5で正式に`emergencyStopped`という名称を決定したため、Round5のコミット1でこの表記を修正した（`emergencyStop`→`emergencyStopped`）。

理由：目的①「エビデンスを残すこと」に照らすと、軽微な仮置きであっても記録がないと、後から発見した際に経緯の確認コストが発生する。今後、将来の機能を見越した仮置き（インフラ定義・命名等）を行う場合は、軽微であってもdecisions-log.mdに一言記録することを徹底する。

## 2026-07-31 ops Lambda（TypeScript）のバンドリング方式を`NodejsFunction`から`lambda.Code.fromAsset`＋ローカルesbuildバンドリングに変更

Round5の`GET/PATCH`系Lambda（`backend/ops/`配下）は、CDKの`NodejsFunction`コンストラクトでは正しくバンドルできないことが判明した。`NodejsFunction`はentryファイルが`projectRoot`（`infra/`）配下にあることを前提としており、`backend/ops/`は`infra/`の兄弟ディレクトリであるためエラーとなる。`projectRoot`をリポジトリルートに変更する対処も試みたが、esbuildの`node_modules`解決パスとの整合が取れず、別の問題が発生した。

最終的に、既存のPython Lambda（週次同期）と同じ方式である`lambda.Code.fromAsset`＋カスタムローカルバンドリングに切り替えて解決した。これに伴い、Lambdaハンドラのエクスポート名を`handler`から`index.handler`に変更した。

理由：ディレクトリ構成（`backend/`と`infra/`が兄弟関係）を維持したまま、TypeScript Lambdaを正しくバンドルするため。

## 2026-07-31（訂正）GSI3 nonKeyAttributesの`emergencyStop`→`emergencyStopped`修正は、DynamoDBの制約によりデプロイ時に差し戻した

同日先に追記した「GSI3 nonKeyAttributesの`emergencyStop`表記は、Round3基盤構築時の仮置きだったことが判明」エントリで「今回のコミット1でこの表記を修正した」と記録したが、実際に`cdk deploy`を実行したところ、DynamoDBの仕様上、既存GSIのnonKeyAttributes（非スループット属性の射影リスト）はin-place変更ができないことが判明し、デプロイが失敗した。

これを受け、`emergencyStop`（旧名）へ差し戻してデプロイを通した。現在の実際のDynamoDBレコードには`emergencyStopped`（新名）フィールドが正しく書き込まれているが、GSI3のnonKeyAttributes射影リストには`emergencyStop`（旧名）が残っており、GSI3経由でこの属性を取得することはできない状態にある。現時点でGSI3を参照するLambdaは存在しないため、運用上の影響はない。GSI3自体を再作成（別名での新規作成等）するタイミングは、reminder Lambda実装予定のRound9に持ち越す。

理由：目的①「エビデンスを残すこと」に照らし、直前のエントリの記述が実際のデプロイ結果と食い違ったまま残ることを避けるため、事実を正確に訂正した。

## 2026-07-31 Round5バックエンドの動作確認で、実データに対して書き込み系APIのテストを実施してしまった。今後、書き込みを伴うテストには専用の合成テストレコードを使う

Round5バックエンド（コミット1〜7）の動作確認において、DynamoDB上の実データ（44件の実レコードの1つ、`subjectId`が`3414dfa0`で始まるレコード）に対し、`PATCH /consent`・`PATCH /acknowledge`・`PATCH /emergency-stop`・`PATCH /memo`の動作確認を直接実施した。この結果、実在するエンジニアの契約更新確認レコードに、テスト用の値（`opsConsentResult`、`opsMemo: "test memo 2026-07-31"`、`emergencyStopped.active`等）が書き込まれた状態になった。

2026-07-28決定で「テストデータ（synthetic/sanitized）は現在状態テーブルから削除する」という運用を確立していたが、今回は逆に、実データそのものに対して書き込みテストを行ってしまい、同じ趣旨（現在状態テーブルを実運用データのみで清潔に保つ）に反する結果となった。

対応として、監査ログテーブルに記録された各操作の`prev`値を用いて、該当レコードをテスト実施前の状態へ復元する。

今後の運用ルール：書き込み系API（PATCH系）の動作確認には、実在する対象者の`subjectId`を使わず、明確にテスト用と識別できる合成レコード（例：`subjectId`にテスト用プレフィックスを付与、`company`名にテストであることが分かる値を使う等）を作成し、確認後に削除する。読み取り専用（GET）の確認はこの限りではない。

## 2026-07-31 team_csv.pyの実装が、決定済みのCSVマッチング仕様（氏名のみ）と異なっていたため、氏名のみのマッチングに修正する

第三者レビュー準備のためのコード確認で、`backend/sync/team_csv.py`の実装が、2026-07-29決定「突合キーは氏名とする」と異なり、`(name, company)`のタプルをマッチングキーとして使っていることが判明した。2026-07-29決定のCSV仕様（社員番号・氏名・所属チーム名の3列）にはcompany列が含まれないため、実際に田中氏から配置されるCSVでは、この実装は氏名の一致有無に関わらず一件もマッチしない（company列が常に空文字列として読み込まれ、実データのcompanyとは一致しないため）。

この食い違いを是正し、2026-07-29決定通り、氏名のみをマッチングキーとする実装に修正する。表記ゆれ等で一致しない場合は、エラーとせずteam欄を空欄として処理を継続する（既存方針を維持）。

理由：決定済みの仕様と実装が食い違ったまま気づかれずに残ると、実際のCSV連携開始時に「エラーは出ないがteamが一件も反映されない」という静かな不具合になる。第三者レビューに回す前に発見・是正できたことは、コードの実体（生出力）を確認する運用の効果を裏付けるものである。

## 2026-07-31 status.tsのステータス導出ロジックで、「未送信のまま契約開始日到達」も「無反応のまま契約開始日到達」に含める

`deriveStatus()`の優先順位では、`!sentAt`（未送信）の判定が契約開始日到達判定より先に評価されていたため、一度も送信されないまま契約開始日を迎えた候補は、「無反応のまま契約開始日到達」ではなく「未送信」のまま表示され続ける実装になっていた。

要件書4.8の「無反応のまま契約開始日到達」の発生条件（「個別確認できないまま契約開始日を迎えた場合」）には、送信済みであることは前提として明記されていない。また「未送信」ステータスには、要件書4.11のハイライト対象（相談したい／更新しない／無反応のまま契約開始日到達）が適用されないため、送信を忘れたまま契約開始日を過ぎた候補が一覧内で視覚的に埋もれるリスクがあった。

これを是正し、契約開始日到達判定を「未送信」判定より先に評価する順序へ変更する。これにより、一度も送信されないまま契約開始日を迎えた候補も「無反応のまま契約開始日到達」（内訳：接触できず）として扱われ、既存のハイライト・四半期モニタリングの対象に含まれる。

理由：目的①（エビデンスを残す）・目的②（属人性を減らす）双方に照らし、送信し忘れという運用上のミスも、他の「個別確認できないまま契約開始日を迎えた」ケースと同様に可視化されるべきと判断した。既存の内訳区分（接触できず／未定のまま）をそのまま流用できるため、新しいサブタイプの追加は不要である。

## 2026-08-01 第三者レビューで発見された、「相談したい」を選んだまま契約開始日を迎えた場合にステータスが更新されない問題を修正する

第三者レビュー（別スレッド）により、`deriveStatus()`の優先順位で`responseType === 'consult'`（相談したい）の判定が契約開始日到達判定より先に評価されるため、本人がオンラインで「相談したい」を選択したまま個別確認の結論が一度も記録されずに契約開始日を迎えた場合、ステータスが永久に`WANTS_CONSULT`のままになり、`PERIOD_REACHED`（無反応のまま契約開始日到達）に遷移しないことが判明した。

これは2026-07-17決定の四半期モニタリングの狙い（「1on1等で本人の意向が事前に拾われている、という前提が将来弱まった場合の早期発見」）と緊張関係にある。「相談したい」を選んだ後に個別確認が滞留したケースは、まさにこの前提が崩れかけている兆候であり、モニタリング対象から漏れるべきではない。

対応として、契約開始日到達判定を`responseType === 'consent'`の後、`responseType === 'consult'`の前に評価する順序へ変更する。内訳（reachedSubtype）は、「相談したい」を経由していた場合も`pending_at_reached`（未定のまま）とする。既存の「接触できず／未定のまま」の2区分をそのまま流用し、新しいサブタイプは追加しない。

理由：オンラインで何らかの意思表示（相談したい）があった以上、一度も接触できなかった場合（no_contact）よりも、保留（pending）に近い状態と判断した。2026-07-31決定（未送信のまま契約開始日到達もPERIOD_REACHEDに含める）と同じ考え方の延長線上にある。

## 2026-08-01 緊急停止APIを、暗黙のトグルから明示的な希望状態を受け取る冪等な方式に変更する

第三者レビューで、`PATCH /emergency-stop`がリクエストボディなしで現在値を反転させる実装（暗黙のトグル）になっており、二重クリックや同時操作時にGet→Updateの間で競合が起きた場合、意図と異なる状態に着地するリスクが指摘された。緊急停止は要件書4.12でMVP必須の安全機能とされており、この種のリスクは看過できない。

このため、リクエストボディで希望状態を明示的に受け取る冪等なAPI（`{ "active": true | false }`）に変更する。2026-07-31決定「緊急停止機能はトグル（再開可能）とする」自体は変更しない（ON/OFFどちらも操作可能という決定は維持し、その呼び出し方法のみを冪等な形に改める）。

理由：フロントエンド（ops-console.html）がまだ実装されていない現時点でAPI契約を変更する方が、フロントエンド実装後に変更するより手戻りが小さい。

## 2026-08-01 チームCSVのヘッダー不正時、「ファイルなし」と区別して異常として扱う

第三者レビューで、`team_csv.py`がCSVファイルは存在するがヘッダー列名が想定と異なる場合（表記ゆれ・作業ミス等）、例外を発生させず`team_map`が空のまま静かに完走することが判明した。ログには「チームCSV読み込み完了: 0件」というINFOログのみが残り、異常を示す手段がなかった。

これは要件書5章「失敗が静かに握りつぶされない設計にする」という明示要件と緊張関係にある。「CSVファイルが存在しない週」（2026-07-29決定により正常系として許容）と「CSVは存在するがフォーマット不正」（異常系）を区別し、後者は最低限WARNING以上のログを出力し、既存の週次同期エラー監視の仕組み（`contract-renewal-sync-errors`アラーム、Round3で構築済み）で検知できる形にする。

理由：ファイル自体の有無で判定する現在の実装は、ファイルは配置されたがヘッダーが壊れている、というより気づきにくい失敗モードを見逃す設計になっていた。

## 2026-08-01（Round9への申し送り）緊急停止中の対象者は、四半期モニタリング集計の対象から除外する方針とする

第三者レビューで、`emergencyStopped`が`deriveStatus()`に一切反映されないため、緊急停止された対象者がそのまま契約開始日を迎えると、通常の「無反応のまま契約開始日到達」と同様に四半期モニタリング（要件書4.8、2026-07-17決定）の母数に混入することが判明した。

緊急停止の典型的な用途（要件書4.12の例：客先都合で契約自体がなくなった場合）は、「1on1等の事前把握が効かなかった」ケースとは性質が異なる。四半期モニタリングの目的が「事前把握という前提が弱まった場合の早期発見」である以上、契約自体が消滅したケースを母数に含めると、この指標の意味が薄まる。

このため、Round9（四半期集計機能）の実装時には、`emergencyStopped.active === true`の対象者を集計対象から除外する方針とする。Round5時点ではステータス導出ロジック自体は変更しない（`emergencyStopped`はアイコン表示用の独立フラグとして扱う、という既存の設計を維持する）。

## 2026-08-01 GET /api/candidatesの名前ソートは、`localeCompare('ja')`では五十音（読み）順にならないことが判明。現状のICU既定順のまま許容し、コードコメントを実態に合わせて修正する

第三者レビュー指摘⑩（ソート順の一貫性・ICU実挙動の未検証）への対応として、実際に`localeCompare('ja')`で日本語の姓20件をソートした結果を確認したところ、期待していた五十音（読み仮名）順にはなっていないことが判明した。

これは、`localeCompare`のようなロケール対応文字列比較が、漢字の「読み」（ふりがな）までは考慮できないためである。同じ漢字表記でも読みは文脈依存であり、Unicode文字列比較の仕組み自体には読みの情報が含まれない。実際の挙動は、ICUの既定の漢字（Han）照合順序（部首・画数に近い基準等）に基づくもので、五十音順とは異なる。

現状の候補者数（実データ44件、想定でも数十〜百数十件）では、営業事務が一覧を目視で探す運用が前提であり、厳密な五十音順でなくても実務上大きな支障はないと判断し、この挙動をそのまま許容する。ただし、`getCandidates.ts`のコメントに「'ja'ロケール順」とだけ書かれていると、五十音順が実現できているという誤解を招くため、実態（ICU既定のHan照合順であり、読み仮名順ではない）を正確に反映する記述に修正する。

将来、厳密な五十音順が必要になった場合は、Excel台帳等から読み仮名（ふりがな）データを別途取得し、それをソートキーとする対応が必要になる。現時点ではこの対応は行わない（バックログ）。

## 2026-08-01 ops-console.htmlのAPI呼び出しを、API Gatewayの直接ドメインから、CloudFront経由の相対パスに変更する

Round5フロントエンド実装後の確認で、`ops-console.html`が`API_BASE`にAPI Gatewayの生ドメイン（execute-api）を直接指定しており、CloudFront（`/api/*` → API Gateway、Round1決定）経由になっていないことが判明した。この結果、フロントエンドとAPIが異なるオリジンとなり、HTTP APIのCORS設定（`allowOrigins: ['*']`）に依存して動作している状態だった。意図的な設計判断ではなく、実装時にCloudFrontの`/api/*`ルーティングの存在を活用し損ねた結果であることをCodeが確認済み。

これを是正し、`API_BASE`を空文字列（相対パス）に変更し、CloudFront経由の同一オリジン通信に統一する。あわせて、HTTP APIのCORS設定（`allowOrigins`）を`['*']`から、CloudFrontの実際のドメイン（`https://d2ule3xvskr65i.cloudfront.net`）に絞り込む。

理由：Round1決定（「新規CloudFrontディストリビューション1つ（`/*` → S3静的アセット、`/api/*` → API Gateway）」）通りの構成に統一する。認証はJWTトークンが実質的な境界だが、不要に広いCORS許可（ワイルドカード）を残す必要はなく、多層防御の観点から絞り込む。

## 2026-08-01 Codeが、Claude Codeのファイルベースのメモリ機能を用いて、確認前に独自の運用ルールファイル（feedback_raw_output_rule.md・MEMORY.md）を作成した事象と対応

Round5フロントエンドの生出力提出について、このスレッドから繰り返し指摘（計4回）を受けた後、Claude Codeが自身の判断で、Claude Code製品のファイルベースのメモリ機能（プロジェクトのGit管理外、`.claude/projects/.../memory/`配下）を用いて、「git diff等のコマンド出力は生出力をそのまま貼ること」という趣旨のメモリファイル2件（`feedback_raw_output_rule.md`・`MEMORY.md`）を作成した。このスレッドへの事前確認は行われていなかった。

調査の結果、以下を確認した。
- 2ファイルはGitリポジトリの管理対象外であり、コミットもされていなかった（実害なし）。
- 内容は、CLAUDE.mdに既に明記されている原則（「省略や簡略化をしないこと」）をCode自身へのリマインダーとして言い換えたものであり、新しい決定事項の追加ではなかった。
- ユーザー画面上に表示された「1件のメモリを取り消した」という操作については、Code自身も内容を特定できなかった（Codeはこの点を正直に「確認できない」と報告した）。

CLAUDE.mdの「未決事項を憶測で補わない。都度、人間に質問する」という原則に照らすと、内容が無害であっても、確認前に永続化する仕組みを独自に使った点は本来の進め方と異なる。ユーザーの判断により、2ファイルは削除することとした。

理由：内容がCLAUDE.mdと重複しており、独自の記憶先を新設する必要性が薄いこと、また「気づきはまずスレッドに報告する」という既存の運用原則を優先したため。
